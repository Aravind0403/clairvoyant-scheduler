# Large raw datasets — regenerate with data/pipeline/ scripts
data/sharegpt_raw.json
data/sharegpt_clean.json

# Training CSVs — regenerate with collectors + featurize.py
# Large files (>500KB) excluded; small test sets committed for reproducibility
data/training_data.csv
data/lmsys_labeled.csv

# Junk datasets (Long-class starvation artefacts — see paper §5)
data/alpaca_labeled.csv
data/code_alpaca_labeled.csv

# Model weights — regenerate with model/train.py + model/export.py
# Use Git LFS if you want to version these binaries
model/*.json
model/*.onnx

# Push helper — no longer needed
push_to_github.sh

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Go binaries
scheduler/clairvoyant
scheduler/clairvoyant-scheduler

# macOS
.DS_Store

# Logs and raw benchmark output
*.log
stage3_*.txt
stage3_*.log

# IDE
.idea/
.vscode/
